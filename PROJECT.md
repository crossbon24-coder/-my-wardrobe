# 개인용 옷장 웹앱 — PROJECT

이 문서는 이 저장소를 이어서 개발하는 작업자의 기준 문서다. 매 작업을 시작할 때 최신 `PROJECT.md`, 실제 코드, `version.json` 및 저장소 지침을 먼저 읽는다. 현재 사용자의 명시적인 지시가 우선하며, 이 문서를 근거로 요청 범위 밖의 구현을 시작하지 않는다.


## 현재 기준 — v3.7 (2026-09-06 개선)

사용자는 최초 인수 보고 후 개발 계속 및 전체 개선을 명시적으로 요청했다. 따라서 최초의 ‘코드 수정 없이 분석만’ 범위는 종료되었으며, 아래 v3.7 현황이 과거 v3.6 분석보다 우선한다. 1장의 개인용·무료·정적 웹앱·데이터 호환성 원칙과 10장의 개발 절차는 계속 적용한다.

### 변경된 동작

- 앱은 계속 단일 index.html이며 GitHub Pages를 사용한다. DB명 wardrobeDB, 버전 1, clothes/outfits 및 id keyPath를 유지했다. 새 DB나 production dependency는 없다.
- MobileNet V2 alpha 0.5, TensorFlow.js 4.22.0, MobileNet 패키지 2.1.1을 유지했다. CDN 스크립트는 사진 분석 시에만 로드한다. 다운로드 실패 후 같은 페이지에서 재시도할 수 있다. 라이브러리 다운로드, 모델 로딩, 추론 및 사진 디코딩에 시간 제한을 추가했다(라이브러리 20초, 모델 load 45초, 추론·사진 30초; tf.ready 자체에는 별도 제한 없음).
- 영문 ImageNet 라벨은 쉼표로 분리한 정확한 별칭으로 대응한다. book jacket 같은 비의류 라벨을 의류로 해석하는 광범위한 substring 확대를 피한다.
- 대분류 점수를 세부종류와 별도로 합산한다. 대분류 자동 추천은 점수 0.25 이상, 다른 대분류 또는 가장 높은 미매핑 후보와의 차이 0.12 이상으로 시작한다.
- 세부종류는 점수 0.40 이상, 다른 세부종류와의 차이 0.20 이상, 해당 대분류 점수의 75% 이상일 때만 추천한다.
- 이 수치는 **실사진으로 보정하지 않은 초기 보수적 정책**이다. 화면의 점수는 정답률이 아니다. 부족한 경우 대분류·세부종류 각각 보류한다.
- 일반 coat 라벨은 아우터까지만, 일반 가방 라벨은 가방까지만 추천한다. 패딩 자동 인식 경로가 생긴 것은 아니다. 패딩을 코트로 강제 확정하는 정책을 제거한 것이다.
- 대분류·세부종류·색상을 사용자가 선택하면 뒤늦은 자동분류 및 재분석이 덮어쓰지 않는다. 재분석은 근거를 갱신하되 수동 선택이 우선이다.
- 색상 중앙값 0 오류를 수정했다. 밝은 저채도 전경의 일괄 제외 및 기타 후보의 경쟁 제외를 제거했다.
- 색상은 전경 120픽셀 이상, 1위 점유율 55% 이상, 1·2위 격차 18% 이상일 때 자동 입력한다. 저채도 올리브 경계, 미지원 주색, 부족한 전경이면 색상 확인 필요로 둔다. 임계값 역시 실사진 검증 전이다.
- 카테고리와 색상은 저장 전에 선택해야 하며, 세부종류는 공란을 허용한다. 불확실한 색은 ‘기타’를 사용자가 선택해 저장할 수 있다.
- 사진별 등록 목록 제외, 개별 파일 읽기 실패 안내, 중복 저장 방지, 분석 중 입력 보호를 추가했다.
- 테스트 결과 JSON을 사진·파일명·메모 없이 내려받을 수 있다. top-5, 분리된 판정 점수, 색상 후보·전경량, 현재 선택 및 사용자 확인 필드를 포함한다. 진단 파일 app 값은 my-wardrobe-diagnostics이며 옷장 백업과 다르다.
- 테스트 기록은 현재 등록 목록 또는 가장 최근에 저장한 일괄 목록을 페이지 메모리에 보관한다. 재시작 시 없어지므로 내보내기가 필요하다. **DB나 백업 스키마에 교정 이력을 추가하지 않았다.** 선택값이 자동 추천 그대로인 경우 사용자 정답으로 확정되었다고 간주하지 말고 userConfirmed를 확인한다.
- DB 쓰기는 request success가 아닌 transaction complete를 기다린다. 복원 시 ID·중복·핵심 형식·모든 이미지의 실제 디코딩을 먼저 확인한다. 이후 두 store를 한 transaction에서 교체해 오류 시 전체를 되돌린다. 취소·검증 실패 시 기존 DB는 유지된다.
- 기존 레코드 추가 필드와 outfits 항목 내용은 보존한다. 알려진 구버전 백업의 앱명·배열·Data URL 구조를 유지한다. 지원하지 않는 카테고리나 잘못된 ID 등은 삭제·추측 변환하지 않고 복원을 중단한다.
- 착용·수정은 최신 DB 레코드를 읽어 변경해 기존 추가 필드 및 착용값을 보존한다. 메모·ID·속성의 HTML 삽입을 이스케이프한다.
- Object URL을 재사용하고 불필요한 URL을 해제한다. 백업 다운로드 링크를 DOM에 부착하고 지연 해제한다.
- 코디 점수에 선택한 날씨와 계절 정보를 실제 반영한다. 신발 선택에는 하의와의 색상 조합도 반영한다. 날씨가 완전히 맞는 옷만 필터링하는 방식은 아니므로 옷장이 제한적이면 맞지 않는 계절의 옷이 추천될 수도 있다.

### 검증

- tests/regression.cjs: 격리된 로컬 Chromium에서 **24개 검사 통과**.
- 합성 이미지 및 통제한 모델 출력으로 대분류·세부종류 보류, 수동 입력과 비동기 결과 충돌, 카키/회색·흰색·기타 판정, 날씨 점수, 일괄 등록·중복 저장 방지, 진단 데이터, CDN 실패 후 수동 입력을 확인했다.
- v3.1 형식의 합성 백업으로 이미지 바이트·착용 기록·추가 필드·기존 outfits 내용의 왕복 보존을 확인했다.
- 손상 이미지, 중복 ID, 잘못된 배열, 동기 쓰기 오류 및 transaction 중복키 오류를 주입해 기존 두 store 보존을 확인했다.
- 실제 사용자의 사진 및 백업, iPhone Safari/홈 화면 모드, 실제 모델 다운로드·사진 추론의 정확도는 **미검증**이다. Chromium 검사를 iPhone 검증으로 표현하지 않는다.
- README.md에 실행 방법과 한계를 기록했다. 테스트용 Playwright는 앱 운영 의존성이 아니다.

### 사용자 보고 사례의 v3.7 상태

| ID | 변경 내용 | 검증 상태 |
| --- | --- | --- |
| R001 패딩 | 일반 외투 라벨로 코트 세부종류를 강제 입력하지 않음 | 합성 모델 출력 정책 통과. 실제 사진에서 아우터 추천 여부·패딩 정답률 미검증 |
| R002 첼시부츠 | 신발 후보 합산, 부츠에 충분한 근거가 있을 때만 세부종류 추천 | 합성 모델 출력 정책 통과. 실제 첼시부츠 사진 미검증 |
| R003 카키 가방 | median 오류 수정, 저채도 회색/카키 경계 보류 | 합성 픽셀 검사 통과. 실제 가방 사진 미검증 |

### 다음 우선순위

1. 사용자 실제 3개 사진과 v3.7 진단 JSON으로 후보·점수·색상 분포를 확인한다. 회색·흰색 대조 사진도 함께 비교한다.
2. iPhone Safari와 홈 화면 모드에서 사진 등록, 다운로드/복원, 수동 입력, 모델 로딩과 처리시간을 검증한다.
3. 실제 사례를 토대로 위 초기 임계값 및 전경 처리를 보정한다. 불확실한 세부종류의 강제 확정을 늘리지 않는다.
4. 샘플이 충분하면 현재 MobileNet 특징벡터 기반 개인화 추천을 비교한다. 새 모델·DB 구조 변경은 별도 영향 검토 후 진행한다.
5. 저장 코디 생성·목록, 아우터 포함 추천은 아직 구현하지 않았다. 사용자 요청 및 실제 활용도에 따라 다음 기능으로 검토한다.

아래 2~9장은 최초 v3.6 인수 당시의 기록이다. ‘현재’ 또는 ‘미구현’이라는 표현이 있어도 v3.7 변경과 충돌하는 경우 위 현황을 따른다.

## 1. 목적과 변경 금지 전제

- 사용자 한 사람이 자신의 실제 옷장을 관리하는 개인용 앱이다. 상용·다중 사용자 서비스로 확장하지 않는다.
- 주 사용 환경은 iPhone Safari 및 홈 화면에 추가한 웹앱이다.
- GitHub repository: `crossbon24-coder/-my-wardrobe`, 기준 branch: `main`.
- GitHub Pages 기반 정적 HTML/CSS/JavaScript 구조를 유지한다. 서버·회원가입·클라우드 DB를 임의로 추가하거나 다른 호스팅으로 이전하지 않는다.
- 완전 무료 운영이 원칙이다. OpenAI API를 포함한 사용량 과금 API 및 유료 서비스를 임의로 도입하지 않는다. API key가 필요한 유료 서비스도 기본적으로 사용하지 않는다.
- 이미지 분석은 TensorFlow.js, 무료 오픈소스 모델, 브라우저 내 처리 등 client-side/on-device 방식을 우선한다.
- 무료 방식의 정확도에 한계가 있으면 한계와 무료 대안을 먼저 설명한다. 모델·서비스 도입은 비용, 라이선스, 다운로드 크기, iPhone 성능과 기존 코드 영향을 확인한 뒤 사용자가 지시한 범위에서만 진행한다.
- 사진과 옷장 데이터는 기존 로컬 IndexedDB를 유지한다. 사진을 외부 분석 서비스에 자동 업로드하지 않는다.
- 기존 IndexedDB 데이터와 JSON 백업·복원 호환성을 반드시 유지한다. DB명·버전·store·keyPath 및 기존 필드 의미를 임의로 변경하지 않는다.
- 일반적인 패션 벤치마크보다 사용자의 실제 옷 사진에 대한 정확도와 수동 수정 부담 감소를 우선한다.
- 자동분류는 틀린 값을 강제로 채우는 것보다 ‘분류 확인 필요’를 선호한다. 대분류와 세부종류의 판단을 분리하는 방향을 검토한다.
- 사용자가 수정한 값은 우선되어야 한다. 향후 비동기 자동분류·재분석이 사용자 선택을 덮어쓰지 않도록 설계한다.
- 폐기된 단순 실루엣 기반 대분류를 기본값으로 복구하지 않는다.

## 2. 인수 기준과 이번 작업 범위

- 분석 기준일: 2026-09-06.
- 분석한 main commit: `29e8a632a4bb9f3741fdc32cf5a8a807a9b06085`.
- 확인된 앱 버전: `3.6`.
- `version.json`: `{"version":"3.6","updatedAt":"2026-09-06T02:30:00+09:00"}`.
- 이번 인수 작업은 전체 파일 읽기, 구현 분석, 이 문서 추가만 수행한다. 앱 코드·모델·DB·백업 형식·버전은 변경하지 않는다.
- 아래 ‘개선 방향’은 제안이며 구현 완료나 개발 착수 승인으로 해석하지 않는다. 다음 구현은 사용자의 후속 지시 후 진행한다.
- 근거 수준: 저장소 전체 정적 분석 및 순수 함수의 제한적 합성 입력 확인. 실제 사용자 사진, 사용자 IndexedDB, iPhone Safari/PWA 실기기 실행, GitHub Pages의 실제 배포 응답은 검증하지 않았다.

## 3. 전체 파일과 실행 구조

| 파일 | 현재 역할 |
| --- | --- |
| `index.html` | 현재 앱. HTML, CSS, 앱 JavaScript가 한 파일에 포함됨 |
| `version.json` | 최신 버전 확인용 메타데이터 |
| `category-v356.js` | 과거 실루엣 기반 분류 스크립트. 파일 내부 주석은 v3.5.2이며 현재 index.html에서 참조하지 않음 |
| `v31.html` | 별도 v3.1 구버전 페이지. 같은 DB명·버전·store를 사용함 |
| `README.md` | 저장소 제목만 존재했음 |
| `PROJECT.md` | 이번 인수 시 추가한 개발 원칙·현황·사례 문서 |

분석 기준 커밋에는 package.json, 빌드 설정, 테스트 파일, manifest, service worker, AGENTS.md 및 별도 프로젝트 디렉터리가 없다.

- 하단 탭: 옷장 / 옷 등록 / 코디 추천.
- iPhone safe-area 및 Apple 홈 화면 웹앱 메타 태그가 있다.
- 외부 스크립트: jsDelivr의 TensorFlow.js 4.22.0, @tensorflow-models/mobilenet 2.1.1.
- 별도 backend 및 유료 API 호출은 현재 코드에서 확인되지 않는다.
- ‘기기 내 분석’은 사진 추론이 로컬이라는 뜻이다. 라이브러리와 모델 다운로드에는 네트워크가 필요할 수 있다. 영구 모델 캐시·오프라인 실행 보장은 구현되어 있지 않다.

## 4. 현재 기능과 실제 범위

- 사진 다중 선택, 등록 대기 목록, 개별 속성 입력, 모두 저장.
- 입력 속성: 대분류·세부종류·색상·계절·격식도·메모.
- 사진은 긴 변 최대 1200px, JPEG quality 0.84로 변환해 Blob으로 저장.
- 카테고리별 옷장 필터, 정보 수정, ‘오늘 입음’.
- 착용 기록은 `wearCount`와 마지막 착용시각 `lastWorn`이다. 날짜별 전체 이력 배열은 없다.
- JSON 백업 생성 및 현재 DB를 백업 내용으로 교체하는 복원.
- 코디 추천은 상의×하의 조합과 신발 1개를 평가한다. 격식도, 사계절 가점, 마지막 착용 후 경과시간, 상·하의 색 조합을 사용한다.
- 날씨 선택 UI와 인자는 있으나 `itemScore(c,t,w)`에서 `w`를 사용하지 않는다.
- 착용 횟수 자체는 추천 점수에 사용되지 않는다. 아우터·가방·액세서리는 추천 조합에 포함되지 않는다.
- `outfits` 조회·개수 표시·백업·복원은 있으나 새 코디를 만드는 저장 동작, 저장 코디 목록 화면은 없다.
- 앱 시작·화면 복귀·수동 요청 시 version.json 확인. 새 버전 안내와 캐시 우회 쿼리를 붙인 새로고침 제공.

## 5. 데이터·백업 호환성 계약

### IndexedDB

- DB명: `wardrobeDB`.
- DB version: `1`.
- Object stores: `clothes`, `outfits`.
- 양쪽 keyPath: `id`.
- 옷 레코드의 기본 필드: `id, image, category, type, color, season, formality, memo, createdAt, wearCount, lastWorn`.
- `image`는 Blob, `formality`는 수치, `createdAt/lastWorn`은 epoch milliseconds(초기 lastWorn은 null).
- 기존 추가 필드는 가능한 한 보존한다. 현재 코드만으로 모든 과거 outfits 레코드의 세부 스키마를 확정할 수 없으므로 사용자 백업 확인 없이 재정의하지 않는다.
- 현재 저장 시 임시 `autoColor, autoRgb, aiStatus, aiRaw, aiConfidence`는 제거된다. 분류 근거나 사용자 교정 이력은 별도로 누적되지 않는다.

### JSON

- 최상위 형식: `{app:"my-wardrobe", version, createdAt, clothes:[...], outfits:[...]}`.
- 옷의 `image`는 Data URL 문자열로 변환하고 복원 시 Blob으로 되돌린다.
- v3.1과 v3.6의 핵심 DB·백업 구조는 코드상 동일하다. 실제 기존 백업 왕복 검증은 미실시다.
- v3.6 복원은 `app`과 `clothes` 배열을 확인하며 outfits가 없으면 빈 배열로 처리한다.
- 복원은 병합이 아니라 교체다. 현재는 두 store를 지운 뒤 항목별로 다시 쓰므로 잘못된 이미지 데이터나 쓰기 실패 시 부분 복원 위험이 있다.
- `all/put/clearS`에는 오류·transaction abort 처리가 충분하지 않고 요청 성공을 transaction 최종 완료와 구분하지 않는다.
- 향후 안전성 개선은 전체 입력 사전검증 및 두 store의 원자적 교체 등을 검토하되 기존 형식을 유지한다.

## 6. v3.6 이미지 카테고리 자동분류

### 실행 흐름

1. 사진을 JPEG Blob으로 변환한다.
2. 별도 JavaScript 색상 분석을 수행한다.
3. `mobilenet.load({version:2, alpha:0.5})`로 모델을 준비한다. 페이지 내 모델·로딩 Promise를 재사용한다.
4. `model.classify(image, 5)`로 상위 5개 ImageNet 분류 결과를 얻는다.
5. 영문 className을 소문자 substring 매칭으로 `MAP`에 대응한다.
6. ‘대분류|세부종류’별 probability를 합산해 가장 높은 묶음을 선택한다.
7. 합산 점수가 0.12 이상이면 대분류와 세부종류를 자동 입력하고, 그 미만이면 ‘분류 확인 필요’로 둔다.
8. 모델 다운로드·분류 실패 시 직접 선택하게 한다. 대분류가 없으면 일괄 저장이 막히고 세부종류는 비워둘 수 있다.

### 매핑의 핵심

| 영문 결과 예시 | 앱 매핑 |
| --- | --- |
| jean / denim | 하의 · 데님 |
| skirt / sarong 등 | 하의 · 세부종류 공란 |
| running shoe / sneaker | 신발 · 스니커즈 |
| loafer | 신발 · 로퍼 |
| cowboy boot / boot | 신발 · 부츠 |
| sandal | 신발 · 샌들 |
| backpack / knapsack 등 | 가방 · 백팩 |
| purse / handbag / mailbag / shopping bag / tote | 가방 · 토트 |
| trench coat / fur coat / overcoat / cloak / poncho | 아우터 · 코트 |
| cardigan | 아우터 · 가디건 |
| jersey / t-shirt / tee shirt | 상의 · 티셔츠 |
| sweatshirt | 상의 · 맨투맨 |
| shirt | 상의 · 셔츠 |

### 확인된 한계

- UI에는 패딩이 있으나 자동분류 MAP에는 패딩 경로가 없다. 단순히 ‘패딩’ 키워드를 추가해도 모델이 그 클래스를 출력하지 않으면 해결되지 않는다.
- 부츠 경로는 있지만 첼시부츠 전용 판별은 없다.
- 대분류 합산과 세부종류 점수가 분리되어 있지 않다. 예를 들어 스니커즈 0.11, 로퍼 0.10, 부츠 0.09이면 신발 합계 0.30을 활용하지 못하고 0.11만 반환한다.
- 반대로 서로 다른 카테고리 후보가 0.13과 0.12로 비슷해도 0.13인 후보를 자동 입력한다. 1·2위 차이와 미매핑 결과의 우세를 검사하지 않는다.
- UI의 퍼센트는 이 규칙의 합산 점수이며 사용자의 옷장에서 검증한 정답률이 아니다.
- `aiRaw`는 선택에 기여한 후보가 아니라 항상 원래 전체 1위 라벨이다. 추천과 설명이 다른 경우가 가능하다.
- 분류 완료 전 사용자가 선택한 값을 후속 자동분류가 덮어쓸 수 있고 ‘다시 분석’도 현재 선택을 대체한다.
- 로딩 실패 후 modelPromise가 null 결과로 남아 같은 페이지에서 재시도되지 않는다.
- 기존 실루엣 스크립트는 현재 사용하지 않는다.

## 7. v3.6 색상 자동판정

1. 이미지를 96×96 canvas로 축소한다.
2. 가장자리 RGB의 채널별 중앙값으로 배경색을 추정한다.
3. 배경과 RGB 유클리드 거리가 30 미만인 픽셀을 제외한다.
4. 매우 밝은 저채도 픽셀(`S<4, L>83`)을 추가 제외한다.
5. 남은 픽셀을 HSL 기준의 색상명 규칙으로 분류한다.
6. 이미지 중심과 채도가 높은 픽셀에 더 큰 가중치를 준다.
7. ‘기타’를 제외한 색상 중 누적 가중치가 가장 높은 색을 고른다. 유효 후보가 없을 때 기타를 반환한다.

### 카키 보정과 한계

- `L<16`이면 검정이다.
- `S<4`이면 흰색·아이보리/크림·회색으로 먼저 반환한다.
- 그 다음 `48≤H<118, L<62` 구간에 카키/올리브 보정이 있다.
- 따라서 S<4의 저채도 올리브는 보정에 도달하지 못한다.
- 배경과 옷의 유사색, 그림자, 광택, 조명·화이트밸런스, 프린트·금속 장식에 영향을 받는다.
- 밝고 채도가 낮은 픽셀을 제외하므로 흰 옷 자체가 제거될 수 있다.
- ‘기타’를 득표 경쟁에서 빼므로 미지원 색이 주색이어도 작은 지원색 영역이 선택될 수 있다.
- 색상의 후보 점유율·1·2위 차이·유효 전경량에 따른 보류 처리가 없다.
- `median(a)`의 `값 || 240`은 유효한 중앙값 0을 240으로 바꾼다. 이는 합성 입력으로 재현한 코드 결함이며 사용자 사진 오분류와의 인과관계는 미확인이다.

## 8. 사용자 사례 누적 — 초기 회귀 검증 목록

실제 사진은 이번 인수 요청에 첨부되지 않았다. 아래는 사용자 보고이며, 현재 버전으로 재실행해 확인한 테스트 통과/실패 기록이 아니다. 각 사례의 정확한 실행 버전·원본 예측값은 추가 확보가 필요하다. 개인 사진을 공개 저장소에 임의로 올리지 않는다.

| ID | 대상 | 사용자 보고 앱 판정 | 정답 | 코드상 검토 | 상태 |
| --- | --- | --- | --- | --- | --- |
| R001 | 패딩 | 아우터 · 코트 | 아우터 · 패딩 | 패딩 자동 매핑 없음, 여러 외투 라벨을 코트로 묶음 | 사진·top-5 확보 후 재현 필요 |
| R002 | 검정 첼시부츠 | 분류 확인 필요 | 신발 · 부츠 | 부츠 매핑은 존재. 미매핑·낮은 점수·후보 분산 여부 확인 필요 | 보류 자체는 잘못된 확정보다 선호 |
| R003 | 저채도 올리브/카키 가방 | 회색 | 카키/올리브 | S<4 조기 반환, 전경 추출·조명 영향 가능. v3.6 일부 보정 | v3.6 사진 재검증 필요 |

### 향후 사례 기록 원칙

- 사례 ID, 사진 식별자, 앱 버전·커밋, 기기/iOS/브라우저 모드.
- 사용자가 확정한 대분류·세부종류·색상 정답.
- 실제 앱 판정과 보류 여부, 사용자 수정값.
- 모델 top-5 원라벨·점수, 매핑 결과, 대분류·세부종류별 근거.
- 색상 전경량·후보 분포 및 주요 RGB/HSL.
- 변경 전후 결과, 실패 원인 가설, 확인된 원인, 아직 미확인인 내용.
- 대분류 정확도·세부종류 정확도·색상 정확도와 함께 잘못된 자동입력률·보류율을 구분한다.
- 확실한 보류를 실패한 강제확정보다 우선하며 ‘보류 → 오답 자동입력’은 악화로 기록한다.
- 기존 사례를 매번 다시 확인하고, 튜닝에 직접 쓰지 않은 별도 사진도 검증한다.
- 초기에는 이 문서의 사례표로 관리할 수 있다. 자동 테스트·별도 저장소·학습 데이터 기능은 아직 구현하지 않았다.

## 9. 다음 개발 우선순위 제안 — 미구현

1. 실제 사진과 모델 top-5를 확보해 3개 사례를 재현하고 기준 기록을 만든다. 사용자 입력을 비동기 결과가 덮어쓰는 문제도 함께 확인한다.
2. MobileNet을 유지하면서 대분류 합산과 세부종류 판단을 분리한다. 두 단계 임계값, 후보 간 격차, 미매핑 결과 우세, 보류 정책을 실제 사례로 조정한다. ‘아우터 / 세부종류 확인 필요’처럼 부분 추천을 우선한다.
3. 색상 분석의 median 0 오류, 흰색 전경 제외, 기타 강제 제외를 먼저 점검한다. 카키/회색 경계는 회색 대조 사진까지 포함해 조정하고 필요하면 자르기·사용자 영역 선택을 검토한다.
4. 기존 데이터 형식을 유지하면서 복원 사전검증·원자성, IndexedDB 오류 처리, Object URL 해제, AI 로딩 재시도 등 iPhone 안정성을 개선한다. 데이터 안전성은 다른 변경의 필수 검토 조건이다.
5. 샘플이 충분히 모이면 현재 MobileNet의 임베딩과 사용자가 확정한 유사 옷 사진을 이용한 로컬 최근접 이웃 추천을 비교 실험한다. 원래 clothes/outfits 스키마를 바꾸지 않고 기존 사진으로 메모리에서 재계산하는 접근부터 검토한다.
6. 위 개선 후에도 부족한 경우에만 무료 대체 모델을 비교한다. 정확도 상승을 사전 보장하지 않으며 비용·라이선스·모델 크기·Safari 메모리·처리시간을 확인한다.
7. 분류 안정화 뒤 날씨가 실제 반영되는 코디 추천, 아우터 조합, 저장 코디 기능 등을 사용자 요청 순서로 검토한다.

## 10. 개발·검증 절차

- 수정 전 최신 main, 전체 관련 파일, PROJECT.md, 버전을 다시 확인한다.
- 요청 범위와 기존 데이터에 미치는 영향을 짧게 명시한다. 단순 개선을 이유로 전면 재작성·모델 교체·DB 개편을 하지 않는다.
- 모델 정확도는 실기기·실제 사진 기준으로 보고한다. 합성 입력 확인과 실제 사진 검증을 구별한다.
- 데이터 처리 변경은 복사한 테스트 DB와 기존 백업으로 확인하며 실제 개인 DB를 테스트 목적으로 초기화하지 않는다.
- 앱 코드 변경 시 관련 회귀 사례와 iPhone Safari/홈 화면 모드를 확인한다. 검증하지 못한 환경은 명시한다.
- 버전 변경이 필요한 배포에서는 APP_VERSION, 화면 표시, version.json을 일치시킨다. 문서만 수정할 때 앱 버전을 올리지 않는다.
- 완료 시 실제 변경 파일, 검증 결과와 미확인 사항을 보고하고 PROJECT.md의 현황·사례를 갱신한다.

## 참고

- [분석 기준 index.html](https://github.com/crossbon24-coder/-my-wardrobe/blob/29e8a632a4bb9f3741fdc32cf5a8a807a9b06085/index.html)
- [분석 기준 version.json](https://github.com/crossbon24-coder/-my-wardrobe/blob/29e8a632a4bb9f3741fdc32cf5a8a807a9b06085/version.json)
- [TensorFlow.js MobileNet 공식 문서](https://github.com/tensorflow/tfjs-models/blob/master/mobilenet/README.md)
- [공식 ImageNet 클래스 목록](https://github.com/tensorflow/tfjs-models/blob/master/mobilenet/src/imagenet_classes.ts)

공식 master 문서는 검토 시점 참고자료이며 앱에서 고정한 npm 버전과 별도로 관리된다.

